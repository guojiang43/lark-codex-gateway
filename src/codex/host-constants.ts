// Internal host identifiers (used for routing, thread prefixes, and workspaces).
// These must NEVER change, because they are encoded into Codex thread IDs.
export const HOST_ID_M4 = "m4";
export const HOST_ID_MACBOOK = "macbook";

// User-facing display names. The single source of truth for all UI labels.
export const DISPLAY_NAME_M4 = "M4mini";
export const DISPLAY_NAME_MACBOOK = "MacBook";
